export interface ItemType {
  key: string;
  name: string;
  width: number | string;
  selected: boolean;
  disable: boolean;
}

export interface ColumnType {
  id: string;
  name: string;
  width: number | string;
  selected: boolean;
  disable: boolean;
}
